# تغییرات — افزودن ویجت «فضای خالی»

**تاریخ:** ۱۴۰۵/۰۵/۲۳ (2026-08-14)
**نسخه:** 2.18.0

یک ویجت تازه به صفحه اصلی اضافه شد که فقط یک فاصله‌ی عمودی خالی ایجاد می‌کند.
مثل بقیه‌ی ویجت‌ها از `/admin/widgets` قابل افزودن، جابه‌جایی (drag & drop)،
فعال/غیرفعال کردن و ویرایش است و می‌تواند چند بار در صفحه تکرار شود.

| # | موضوع | فایل اصلی |
|---|---|---|
| [۱](#۱--کامپوننت-فروشگاه) | کامپوننت فروشگاه | `components/store/SpacerSection.tsx` |
| [۲](#۲--چرا-ارتفاع-با-متغیر-css-اعمال-میشود) | روش اعمال ارتفاع | `app/globals.css` |
| [۳](#۳--اتصال-به-صفحه-اصلی) | اتصال به صفحه اصلی | `components/store/StoreHomePageClient.tsx` |
| [۴](#۴--پنل-ادمین) | پنل ادمین | `app/admin/widgets/page.tsx`, `[id]/page.tsx` |
| [۵](#۵--دیتابیس) | دیتابیس | `prisma/schema.prisma` |

---

## خلاصه

- نوع ویجت: `SPACER` — با برچسب «فضای خالی» و آیکن ↕️ در لیست ویجت‌ها.
- تنظیمات: ارتفاع دسکتاپ و ارتفاع موبایل (هرکدام ۰ تا ۴۰۰ پیکسل).
- هیچ عنصر دیده‌شدنی رندر نمی‌کند؛ فقط جای خالی اشغال می‌کند و با
  `aria-hidden="true"` برای صفحه‌خوان‌ها نامرئی است.

---

## ۱ — کامپوننت فروشگاه

`components/store/SpacerSection.tsx`

ساختار config:

| کلید | معنی | بازه |
|---|---|---|
| `height` | ارتفاع در دسکتاپ (عرض ≥ ۷۶۸px) | ۰ تا ۴۰۰ پیکسل |
| `heightMobile` | ارتفاع در موبایل — اگر `null` باشد از ارتفاع دسکتاپ پیروی می‌کند | ۰ تا ۴۰۰ پیکسل یا `null` |

`normalizeSpacerConfig()` هر ورودی نامعتبری را امن می‌کند: عدد خارج از بازه کلمپ
می‌شود، مقدار غیرعددی به پیش‌فرض برمی‌گردد و `undefined` به `null` تبدیل می‌شود.
این تابع **هم در زمان رندر و هم در فرم ادمین** صدا زده می‌شود، پس حتی اگر مقدار
خرابی مستقیم در دیتابیس نوشته شود، صفحه اصلی سالم می‌ماند.

### چرا `SPACER_DEFAULT.heightMobile` برابر `null` است

خروجی `normalizeSpacerConfig({})` — یعنی ویجت تازه‌ساخته‌شده که هنوز ذخیره نشده —
`heightMobile: null` است. اگر پیش‌فرض عدد می‌بود (مثلاً ۳۲)، سه رفتار متفاوت
پیش می‌آمد:

- سایت برای ویجت تازه، موبایل را برابر دسکتاپ نشان می‌داد،
- فرم ادمین قبل از رسیدن داده، ۳۲ نشان می‌داد،
- دکمه‌ی «بازگرداندن به پیش‌فرض» مقدار سومی می‌گذاشت.

با `null` هر سه یکی می‌شوند. این مورد حین تست پیدا شد.

---

## ۲ — چرا ارتفاع با متغیر CSS اعمال می‌شود

مقدار ارتفاع در زمان اجرا از دیتابیس می‌آید، اما Tailwind کلاس‌ها را در زمان
build می‌سازد؛ پس `h-[${height}px]` کار نمی‌کند. از طرفی ارتفاع باید بین موبایل و
دسکتاپ فرق کند و media query را نمی‌شود در `style` اینلاین نوشت.

راه‌حل: کامپوننت فقط دو متغیر CSS را اینلاین ست می‌کند و سوییچ بین آن‌ها در
`globals.css` انجام می‌شود:

```tsx
<div className="widget-spacer" aria-hidden="true"
     style={{ "--spacer-h": `${height}px`, "--spacer-h-mobile": `${mobile}px` }} />
```

```css
.widget-spacer { height: var(--spacer-h-mobile, 32px); flex-shrink: 0 }
@media (min-width: 768px) {
  .widget-spacer { height: var(--spacer-h, 64px) }
}
```

- مقدار fallback داخل `var()` تضمین می‌کند اگر ویجت بدون config رندر شد،
  ارتفاع صفر نشود.
- `flex-shrink: 0` جلوگیری می‌کند از اینکه در چیدمان‌های flex آینده فشرده شود.
- قانون در `globals.css` نوشته شد (نه `<style>` داخل کامپوننت) چون این ویجت
  می‌تواند چند بار در یک صفحه تکرار شود و نباید CSS تکراری تزریق کند.

نقطه‌ی شکست ۷۶۸ پیکسل عمداً همان `md` تیلویند است تا با بقیه‌ی سایت هم‌خوان باشد.

---

## ۳ — اتصال به صفحه اصلی

`StoreHomePageClient.tsx` — یک `dynamic import` و یک `case` مثل بقیه‌ی ویجت‌ها:

```tsx
case "SPACER":
  return <Spacer key={key} config={config} />;
```

### نکته‌ی مهم: فاصله‌ی ۴۸ پیکسلی موجود

ظرف ویجت‌های صفحه اصلی کلاس `space-y-12` دارد، یعنی از قبل بین هر دو ویجت
۴۸ پیکسل فاصله هست. بنابراین فاصله‌ی نهایی بین ویجت قبلی و بعدی برابر است با:

```
۴۸ + ارتفاع ویجت + ۴۸
```

این موضوع در پنل ادمین با عدد دقیق به مدیر نمایش داده می‌شود تا انتظارش با
نتیجه یکی باشد.

---

## ۴ — پنل ادمین

### لیست ویجت‌ها (`app/admin/widgets/page.tsx`)

یک رکورد به `WIDGET_META` اضافه شد (برچسب «فضای خالی»، آیکن ↕️، رنگ خاکستری) و
`SPACER` به یونیون `WidgetType`. بقیه‌ی امکانات — drag & drop، سوییچ فعال/غیرفعال
و حذف — چون از همان لیست می‌آیند، خودکار کار می‌کنند.

### صفحه‌ی ویرایش (`app/admin/widgets/[id]/page.tsx`)

کامپوننت `SpacerEditor` اضافه شد با:

- دو اسلایدر (گام ۴ پیکسل) به‌همراه ورودی عددی دقیق برای دسکتاپ و موبایل.
- سوییچ «در موبایل هم همان ارتفاع دسکتاپ باشد» — روشن بودنش یعنی
  `heightMobile: null` و ورودی موبایل غیرفعال می‌شود.
- دکمه‌ی «بازگرداندن به مقادیر پیش‌فرض».
- **پیش‌نمایش با مقیاس واقعی**: دو بلوک «ویجت قبلی/بعدی»، دو نوار آبی که همان
  فاصله‌ی ۴۸ پیکسلی صفحه اصلی را نشان می‌دهند و بلوک خط‌چین وسط که دقیقاً به
  اندازه‌ی ارتفاع تنظیم‌شده است.
- جعبه‌ی راهنما با جمع نهایی فاصله برای دسکتاپ و موبایل.

`SPACER` به `SUPPORTED`، به `pageDesc` و به شاخه‌های بارگذاری/ذخیره اضافه شد.
هنگام ذخیره، config از `normalizeSpacerConfig()` رد می‌شود.

---

## ۵ — دیتابیس

مقدار `SPACER` به enum `WidgetType` اضافه شد.
مایگریشن: `prisma/migrations/20260814095104_add_spacer_widget/`

جدول `Widget` تغییری نکرد؛ تنظیمات در همان ستون `config` (Json) ذخیره می‌شود.

---

## تست انجام‌شده

`tsc --noEmit` بدون خطا. تست‌ها روی سرور واقعی و با مرورگر انجام شد:

| سناریو | نتیجه |
|---|---|
| رندر در صفحه اصلی با `{height:200, heightMobile:80}` | ارتفاع اندازه‌گیری‌شده در دسکتاپ دقیقاً ۲۰۰px ✓ |
| همان ویجت در عرض ۳۹۰px | ارتفاع اندازه‌گیری‌شده دقیقاً ۸۰px ✓ |
| مقدار خارج از بازه در دیتابیس (`9999` و `-50`) | هنگام رندر به ۴۰۰ و ۰ کلمپ شد ✓ |
| `heightMobile: null` | موبایل از ارتفاع دسکتاپ پیروی کرد ✓ |
| افزودن ویجت از API ادمین با `type: "SPACER"` | ساخته شد، config خالی با پیش‌فرض رندر شد ✓ |
| ذخیره از مسیر ادمین (PUT) | config درست ذخیره شد ✓ |
| صفحه‌ی ویرایش در مرورگر | اسلایدرها، سوییچ، پیش‌نمایش و جمع فاصله درست ✓ |

---

## فایل‌های تغییریافته

```
components/store/SpacerSection.tsx        ← جدید
app/globals.css                           ← قانون .widget-spacer
components/store/StoreHomePageClient.tsx  ← import + case
app/admin/widgets/page.tsx                ← WIDGET_META + یونیون نوع
app/admin/widgets/[id]/page.tsx           ← SpacerEditor + state/load/save/SUPPORTED/pageDesc
prisma/schema.prisma                      ← SPACER در enum WidgetType
prisma/migrations/20260814095104_add_spacer_widget/
```

## افزودن ویجت بعدی (چک‌لیست)

۱. `prisma/schema.prisma` → مقدار جدید در `enum WidgetType` + migration
۲. `components/store/<Name>Section.tsx` → کامپوننت فروشگاه
۳. `StoreHomePageClient.tsx` → `dynamic import` + `case`
۴. `app/admin/widgets/page.tsx` → یونیون `WidgetType` + رکورد `WIDGET_META`
۵. `app/admin/widgets/[id]/page.tsx` → کامپوننت ادمین + state + شاخه در
   بارگذاری + شاخه در `handleSave` + `SUPPORTED` + `pageDesc` + رندر شرطی
