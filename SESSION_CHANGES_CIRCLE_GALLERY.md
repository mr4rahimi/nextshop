# تغییرات — ویجت «گالری دایره‌ای»

**تاریخ:** ۱۴۰۵/۰۵/۲۳ (2026-08-14)
**نسخه:** 2.21.0

ویجت تازه‌ای برای نمایش مجموعه‌ای از تصاویر گرد لینک‌دار در یک اسلایدر، با قطر،
پس‌زمینه و رفتار قابل تنظیم از ادمین.

| # | موضوع | فایل اصلی |
|---|---|---|
| [۱](#۱--تعداد-خودکار-از-روی-قطر) | تعداد خودکار از روی قطر | `components/store/CircleGallerySection.tsx` |
| [۲](#۲--جای-فلشها) | جای فلش‌ها در موبایل و دسکتاپ | همان فایل |
| [۳](#۳--پسزمینه-و-رنگ-متن) | پس‌زمینه و رنگ خودکار متن | همان فایل |
| [۴](#۴--پنل-ادمین) | پنل ادمین | `app/admin/widgets/[id]/page.tsx` |

---

## خلاصه

- نوع ویجت: `CIRCLE_GALLERY` با برچسب «گالری دایره‌ای» و آیکن ⭕
- هر آیتم: تصویر (آپلود مستقیم) + عنوان اختیاری + لینک اختیاری
- تصاویر خودکار به شکل دایره بریده می‌شوند (`rounded-full` + `object-cover`)،
  پس نیازی نیست ادمین تصویر مربعی آماده کند

### config

| کلید | معنی |
|---|---|
| `items` | آرایه‌ی `{ imageUrl, title, linkUrl }` |
| `diameter` | قطر دایره بر حسب پیکسل (۴۸ تا ۲۰۰) |
| `heading` / `subheading` | عنوان و زیرعنوان بخش (اختیاری) |
| `showTitle` | نمایش عنوان زیر هر دایره |
| `showArrows` | نمایش فلش‌های ناوبری |
| `autoplay` / `autoplaySeconds` | اسلاید خودکار و فاصله‌اش (۱ تا ۱۵ ثانیه) |
| `bgType` | `none` / `solid` / `gradient` |
| `bgColor`, `bgGradientFrom`, `bgGradientTo`, `bgGradientDir` | رنگ‌بندی پس‌زمینه |

`normalizeCircleGalleryConfig()` هم در رندر و هم در فرم ادمین صدا زده می‌شود:
عدد خارج از بازه کلمپ می‌شود، رنگ بدون فرمت `#rrggbb` به پیش‌فرض برمی‌گردد،
`bgType` نامعتبر به `none` تبدیل می‌شود و آیتم‌های بدون تصویر حذف می‌شوند.

---

## ۱ — تعداد خودکار از روی قطر

خواسته این بود که فقط یکی از «سایز» و «تعداد» تنظیم شود و دیگری خودکار دربیاید.
تنظیم، **قطر** است و تعداد خودکار حساب می‌شود — اما نه با محاسبه‌ی دستی در
جاوااسکریپت:

```js
slidesPerView: "auto"   // عرض هر اسلاید = قطر دایره
```

با این تنظیم، خود مرورگر در همان لحظه هرچند دایره که در عرض موجود جا می‌شود نشان
می‌دهد. مزیتش این است که هنگام تغییر اندازه‌ی پنجره یا چرخاندن گوشی، هیچ محاسبه‌ی
دستی و هیچ پرشی در چیدمان رخ نمی‌دهد.

### حفاظ موبایل

اگر ادمین قطر بزرگی انتخاب کند (مثلاً ۲۰۰ پیکسل)، در گوشی فقط یک دایره جا می‌شد.
برای همین عرض هر دایره `min(قطر, ۳۸vw)` است: در صفحه‌های باریک دایره‌ها کمی
کوچک‌تر می‌شوند تا همیشه دست‌کم دو دایره و نیم در دید باشد.

### اندازه‌گیری واقعی

در ویوپورت ۵۰۰ پیکسل:

| قطر | تعداد کامل در دید |
|---|---|
| ۶۴px | ۵ دایره |
| ۹۶px | ۴ دایره |
| ۱۶۰px | ۲ دایره |

---

## ۲ — جای فلش‌ها

یک جفت دکمه که با CSS جایشان عوض می‌شود، نه دو جفت جدا:

- **موبایل:** زیر دایره‌ها، وسط‌چین (`mt-5 flex justify-center gap-3`)
- **دسکتاپ:** دو طرف اسلایدر (`md:absolute md:top-1/2 md:-translate-y-1/2` با
  `md:right-0` و `md:left-0`)

اندازه‌گیری در ۱۴۴۰ پیکسل: جعبه از ۶۴ تا ۱۳۶۸ و فلش‌ها دقیقاً روی همان لبه‌ها
(۱۳۲۸ و ۶۴). برای اینکه فلش‌ها روی دایره‌ها نیفتند، در دسکتاپ به اسلایدر
`md:px-14` داده شده.

با `watchOverflow: true`، وقتی همه‌ی دایره‌ها جا می‌شوند سوایپر خودش فلش‌ها را
پنهان می‌کند — یعنی وقتی چیزی برای پیمایش نیست، دکمه‌ی بی‌فایده نشان داده نمی‌شود.
کشیدن با انگشت در هر حالت فعال است، حتی وقتی فلش‌ها از ادمین خاموش باشند.

---

## ۳ — پس‌زمینه و رنگ متن

پس‌زمینه یک جعبه‌ی گوشه‌گرد داخل عرض محتواست (نه تمام‌عرض صفحه) تا با بقیه‌ی
ویجت‌ها هماهنگ بماند. سه حالت: بدون پس‌زمینه، رنگ ساده، گرادینت با شش جهت.

### مشکلی که حین تست پیدا شد

سایت در حالت شب بود و ادمین یک گرادینت روشن انتخاب کرده بود؛ عنوان و متن‌ها با
کلاس `dark:text-white` رندر می‌شدند، یعنی **متن روشن روی زمینه‌ی روشن** — عملاً
ناخوانا.

راه‌حل: وقتی پس‌زمینه‌ی سفارشی هست، رنگ متن دیگر از تم سایت نمی‌آید بلکه از
روشناییِ خودِ پس‌زمینه حساب می‌شود:

```ts
export function circleGalleryTextColor(cfg) {
  if (cfg.bgType === "none") return undefined;   // همان کلاس‌های تم سایت
  const l = cfg.bgType === "solid"
    ? luminance(cfg.bgColor)
    : (luminance(cfg.bgGradientFrom) + luminance(cfg.bgGradientTo)) / 2;
  return l > 0.45 ? "#111827" : "#f9fafb";
}
```

`luminance()` روشنایی نسبی sRGB را حساب می‌کند (همان فرمول استاندارد دسترسی‌پذیری).
برای گرادینت میانگین دو سر گرادینت گرفته می‌شود. همین تابع در پیش‌نمایش ادمین هم
استفاده می‌شود تا آنچه ادمین می‌بیند با نتیجه یکی باشد.

---

## ۴ — پنل ادمین

- **اسلایدر قطر** به‌همراه یک جدول کوچک که تعداد تقریبی دایره‌ها را در چهار عرض
  رایج (موبایل، تبلت، لپ‌تاپ، دسکتاپ) نشان می‌دهد. تأکید شده که عدد دقیق را خود
  مرورگر در لحظه حساب می‌کند.
- **پس‌زمینه**: سه دکمه (بدون / ساده / گرادینت) و بسته به انتخاب، انتخابگر رنگ یا
  دو رنگ گرادینت + جهت.
- **سه سوییچ**: نمایش عنوان زیر دایره، نمایش فلش‌ها، اسلاید خودکار (با اسلایدر
  زمان‌بندی که فقط با روشن بودن اسلاید خودکار ظاهر می‌شود).
- **پیش‌نمایش زنده** با همان قطر و همان پس‌زمینه و همان رنگ متن.
- **مدیریت تصاویر**: آپلود مستقیم، جابه‌جایی با ↑ ↓، حذف، و برای هر تصویر عنوان
  و لینک. عنوان به‌عنوان متن جایگزین تصویر (alt) هم استفاده می‌شود.

---

## تست انجام‌شده

`tsc --noEmit` بدون خطا. تست با مرورگر واقعی:

| سناریو | نتیجه |
|---|---|
| رندر با ۶ تصویر و قطر ۹۶ | دایره‌ها دقیقاً ۹۶×۹۶ پیکسل ✓ |
| تغییر قطر ۶۴ / ۹۶ / ۱۶۰ در ویوپورت ۵۰۰px | تعداد در دید ۵ / ۴ / ۲ — خودکار ✓ |
| موبایل ۳۹۰px | جعبه‌ی گرادینت، عنوان‌ها، دایره‌ها و فلش‌های زیر اسلایدر ✓ |
| دسکتاپ ۱۴۴۰px با سرریز | فلش‌ها دقیقاً روی دو لبه‌ی جعبه (۱۳۲۸ و ۶۴) ✓ |
| دسکتاپ بدون سرریز | فلش‌ها خودکار پنهان شدند (`watchOverflow`) ✓ |
| گرادینت روشن در حالت شب سایت | متن‌ها تیره و خوانا شدند ✓ |
| config خراب (`diameter: 9999`، `bgType: "weird"`، آیتم بدون تصویر) | هنگام رندر به ۲۰۰، بدون پس‌زمینه و حذف آیتم ناقص تبدیل شد ✓ |
| صفحه‌ی ویرایش در مرورگر | همه‌ی بخش‌ها و جدول تعداد خودکار درست ✓ |

ویجت تستی از دیتابیس حذف شد.

---

## فایل‌های تغییریافته

```
components/store/CircleGallerySection.tsx   ← جدید
components/store/StoreHomePageClient.tsx    ← import + case
app/admin/widgets/page.tsx                  ← WIDGET_META + یونیون نوع
app/admin/widgets/[id]/page.tsx             ← CircleGalleryEditor + اتصالات
prisma/schema.prisma                        ← CIRCLE_GALLERY در enum WidgetType
prisma/migrations/20260814…_add_circle_gallery_widget/
```
