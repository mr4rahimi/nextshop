# تغییرات — پیمایش دسته‌بندی و ارتفاع بنر تمام‌عرض

**تاریخ:** ۱۴۰۵/۰۵/۲۱ (2026-08-12)
**نسخه:** 2.16.0

دو ایراد گزارش‌شده روی لوکال بررسی و برطرف شد، سپس روی سایت دموی `9dm.ir` مستقر شد.

| # | موضوع | فایل اصلی |
|---|---|---|
| [۱](#مشکل-۱--فقط-۱۲-محصول-در-صفحه-دستهبندی) | صفحه دسته‌بندی فقط ۱۲ محصول نشان می‌داد | `components/store/categories/CategoryPageClient.tsx` |
| [۲](#مشکل-۲--ارتفاع-ثابت-ویجت-بنر-تمامعرض) | ارتفاع ویجت بنر تمام‌عرض ثابت شده بود | `components/store/FullBannerSection.tsx` |

---

# مشکل ۱ — فقط ۱۲ محصول در صفحه دسته‌بندی

## نشانه

در `/categories/printer` با ۷۵ محصول، فقط ۱۲ محصول نمایش داده می‌شد و اسکرول
هیچ محصول جدیدی لود نمی‌کرد. روی مهام‌پرینت با بیش از ۵۰۰ محصول در همان دسته
نیز همین رفتار دیده می‌شد.

## علت

`CategoryPageClient.tsx` عنصر sentinel اسکرول بی‌نهایت را رندر می‌کرد، ولی افکت
`IntersectionObserver` که باید آن را رصد کند **وجود نداشت**. `sentinelRef` تعریف
شده بود و به یک `<div>` وصل می‌شد، اما هیچ‌کس observe نمی‌کرد.

همان افکت در دو کامپوننت خواهر سالم بود:

- `components/store/products/ProductsPageClient.tsx:252`
- `components/store/brands/BrandPageClient.tsx:243`

به همین دلیل فقط صفحه دسته‌بندی خراب بود. افکت احتمالاً در کامیت `52127b8`
(«SEO: landing page chips on category, fix infinite URL sync loop») هنگام رفع
حلقه‌ی بی‌نهایت همگام‌سازی URL حذف شده است.

## اصلاحات

### ۱) بازگرداندن observer

افکت `IntersectionObserver` اضافه شد با این جزئیات:

- `rootMargin: "400px 0px"` — کمی زودتر از رسیدن به انتهای لیست لود می‌کند تا
  اسکرول بی‌وقفه بماند.
- پرچم `fired` داخل هر observer — هر observer فقط یک‌بار صفحه بعد را می‌گیرد.
- `loadingRef` — جلوگیری از فچ هم‌زمان.
- `loading` در آرایه وابستگی‌ها — تا وقتی فچ تمام شد observer دوباره ساخته شود و
  اگر sentinel هنوز در دید است، صفحه بعدی زنجیره‌وار لود شود.

> **نکته پیاده‌سازی:** فچ عمداً بیرون از `setPage(prev => …)` صدا زده می‌شود.
> React در حالت StrictMode تابع به‌روزرسان state را دوبار اجرا می‌کند و اگر
> عارضه‌ی جانبی داخلش باشد، هر صفحه دوبار فچ می‌شود.

### ۲) مرز اسکرول / صفحه‌بندی

```diff
- const useInfiniteScroll = page <= INFINITE_SCROLL_LIMIT;
+ const useInfiniteScroll = page < INFINITE_SCROLL_LIMIT;
```

با `<=` صفحه یازدهم هم خودکار لود می‌شد و تازه بعد از آن صفحه‌بندی ظاهر می‌شد.
حالا صفحات ۱ تا ۱۰ با اسکرول لود می‌شوند و از صفحه ۱۰ صفحه‌بندی عددی نمایش
داده می‌شود — همان رفتاری که مدنظر بود.

### ۳) صفحه‌بندی قابل کراول (یک حفره‌ی واقعی سئو)

صفحه‌بندی با `<button onClick>` ساخته شده بود، یعنی **گوگل هیچ راهی برای رسیدن
به محصولات بعد از ۱۲ تای اول نداشت**. `?page=N` سمت سرور رندر و ایندکس می‌شد،
ولی هیچ لینکی به آن اشاره نمی‌کرد.

- همه دکمه‌ها به `<Link href>` با نشانی واقعی تبدیل شدند.
- `rel="next"` / `rel="prev"` و `aria-current="page"` اضافه شد، داخل
  `<nav aria-label="صفحه‌بندی">`.
- زیر گرید یک لینک واقعی «نمایش محصولات بیشتر» به `?page=N+1` اضافه شد. با
  جاوااسکریپت `preventDefault` می‌شود و محصولات را append می‌کند؛ بدون
  جاوااسکریپت (و برای خزنده) یک لینک معمولی به صفحه بعد است.
- تابع کمکی `hrefFor(p)` که از همان `buildParams` استفاده می‌کند تا نشانی
  صفحه‌بندی دقیقاً با فیلترهای فعال هم‌خوان بماند.

### ۴) پنجره متحرک شماره صفحات

قبلاً شماره صفحات همیشه ثابت از `INFINITE_SCROLL_LIMIT + 1` شروع می‌شد (یعنی
۱۱ تا ۱۷)، پس کاربرِ صفحه ۳۰ هم فقط ۱۱..۱۷ را می‌دید. تابع `pageWindow()` اضافه
شد که پنجره‌ای هفت‌تایی حول صفحه فعلی می‌سازد.

### ۵) حذف رفت‌وبرگشت اضافه به سرور

```diff
- router.replace(next, { scroll: false });
+ window.history.replaceState(null, "", next);
```

افکت همگام‌سازی URL با `router.replace` باعث می‌شد Next.js کل کامپوننت سروری
صفحه را دوباره رندر کند (شامل `fetchCatalog` و کوئری‌های دیتابیس). محصولات
به‌هرحال از `/api/products` گرفته می‌شوند، پس این رندر مجدد صرفاً هدررفت بود —
و با تعمیر اسکرول بی‌نهایت، در هر جلسه تا ۱۰ بار تکرار می‌شد. روی مهام‌پرینت با
۵۰۰+ محصول در یک دسته این بار قابل توجه است.

`window.history.replaceState` الگوی رسمی App Router برای به‌روزرسانی سطحی نشانی
است. نشانی همچنان قابل اشتراک‌گذاری و بوکمارک است و بارگذاری مستقیم همان URL
کاملاً سمت سرور رندر می‌شود. `useRouter` دیگر لازم نبود و حذف شد.

## اثر روی سئو

هیچ تغییری در `lib/catalog.ts` و سیاست ایندکس داده نشد. رفتار قبلی حفظ شد:

| حالت | robots | canonical |
|---|---|---|
| صفحه پایه | `index, follow` | خودارجاع |
| صفحه‌بندی خالص `?page=N` | `index, follow` | `?page=N` (خودارجاع) |
| فیلتر / مرتب‌سازی / قیمت | `noindex, follow` | صفحه پایه |

تغییر خالص سئو **مثبت** است: مسیر کراول از صفحه ۱ به ۲ به ۳ … حالا وجود دارد،
درحالی‌که قبلاً هیچ لینکی به صفحات بعدی نبود.

## بررسی انجام‌شده

- `tsc --noEmit` تمیز.
- `?page=1/2/3` هرکدام ۱۲ محصول SSR می‌کنند.
- صفحه ۳ لینک `rel="next"` به `page=4` دارد.
- صفحه ۷ (آخر، ۷۵ محصول) لینک بعدی ندارد.
- `robots: index, follow` و canonical خودارجاع دست‌نخورده.

## ⚠️ کار باقی‌مانده

`ProductsPageClient.tsx` و `BrandPageClient.tsx` اسکرول بی‌نهایتشان سالم است،
ولی **همان حفره‌ی سئویی صفحه‌بندی با `<button>`** و همان پنجره‌ی ثابت شماره
صفحات را دارند. عمداً دست نخوردند چون خارج از مشکل گزارش‌شده بودند. اعمال همین
الگو روی آن دو یک کار مستقل و کوچک است.

---

# مشکل ۲ — ارتفاع ثابت ویجت بنر تمام‌عرض

## نشانه

ویجت `FULL_BANNER` قبلاً ارتفاعش را از خود تصویر می‌گرفت، ولی ارتفاعش ثابت شده
بود. مهام‌پرینت این ویجت را به‌عنوان ویجت اول با یک تصویر متحرک گذاشته و بنر
بریده و ناقص نمایش داده می‌شد.

## علت

**`globals.css` مقصر نبود.** قوانین ارتفاعی که آنجا اضافه شده بود همه به
`.suggestionSwiper` و `.story-*` محدودند و هیچ‌کدام بنر را هدف نمی‌گیرند.

مقصر کامیت `25277e2` («a11y + perf: banner alt, aria-labels, redis fix…») بود:

```diff
- <div className="group relative overflow-hidden rounded-[2rem] shadow-lg shadow-black/10 dark:shadow-black/40 …">
-   <img className="w-full h-auto object-cover …" />
+ <div className="group relative aspect-[1774/426] overflow-hidden rounded-[2rem] shadow-lg ...">
+   <Image fill className="object-cover …" />
```

نسبت `1774/426` روی کانتینر قفل شده بود و `fill` + `object-cover` هر تصویری با
نسبت متفاوت را می‌بُرید.

### باگ دومِ همان کامیت

کلاس‌های کانتینر به‌صورت **لفظی** به `shadow-lg ...` خلاصه شده بودند — یعنی
`...` واقعاً داخل رشته‌ی `className` نوشته شده بود. سایه‌ی حالت تاریک و افکت‌های
hover (`hover:shadow-2xl`, `hover:-translate-y-1`) از دست رفته بودند.

## اصلاح

```diff
- <div className="group relative aspect-[1774/426] overflow-hidden rounded-[2rem] shadow-lg ...">
+ <div className="group relative overflow-hidden rounded-[2rem] shadow-lg shadow-black/10 dark:shadow-black/40 transition-all duration-500 hover:shadow-2xl hover:shadow-black/20 dark:hover:shadow-black/60 hover:-translate-y-1">
    <Image
      src={safeUrl}
      alt={alt || "بنر"}
-     fill
+     width={0}
+     height={0}
      sizes="(min-width: 1024px) 1200px, 100vw"
-     className="object-cover …"
+     className="w-full h-auto object-cover …"
    />
```

`width={0} height={0}` به‌علاوه‌ی `w-full h-auto` یعنی نسبت ثابتی تحمیل نمی‌شود
و مرورگر ابعاد ذاتی خود فایل را ملاک می‌گیرد.

کامپوننت `<Image>` نگه داشته شد (نه `<img>` خام): `next.config.ts:60` الان
`unoptimized: true` دارد، پس عملاً همان `<img>` رندر می‌شود و انیمیشن GIF هم
سالم می‌ماند؛ ولی اگر روزی `unoptimized` برگردانده شود این نسخه سازگار می‌ماند.

یک کامنت در کد گذاشته شد که چرا نباید دوباره به `aspect` ثابت + `fill` برگردد.

## ⚠️ نکته باقی‌مانده

چون ابعاد تصویر از قبل مشخص نیست، فضای بنر پیش از لود شدن رزرو نمی‌شود و ممکن
است پرش چیدمان (CLS) دیده شود — دقیقاً مثل رفتار قبل از کامیت `25277e2`.

راه‌حل بهتر برای آینده: هنگام آپلود در `app/api/admin/upload/route.ts` ابعاد
تصویر خوانده و در config ویجت ذخیره شود تا هم ارتفاع طبیعی بماند هم CLS صفر
شود. نیازمند fallback برای بنرهای موجود است.

## بررسی انجام‌شده

- `tsc --noEmit` تمیز.
- خروجی HTML صفحه اصلی لوکال: `<img class="w-full h-auto object-cover …">`
  بدون هیچ محدودیت ارتفاعی.
- `DoubleBannerSection.tsx` از اول `w-full h-auto` بود و دست نخورد.
- `aspect` های عمدی سایر ویجت‌ها لمس نشدند: `Hero.tsx` (`1400/500`) و
  `LatestArticlesSection.tsx` (`16/10`).

---

# چنج‌لاگ کاربران

`app/admin/changelog/page.tsx` دو تغییر داشت:

### ۱) نسخه ۲.۱۶.۰ اضافه شد

۹ مورد شامل دو اصلاح بالا و بهبودهای جانبی.

### ۲) بازنویسی متن‌های قبلی

این صفحه را کاربران سایت‌ها می‌بینند و متن‌ها روایت می‌کردند که «قبلاً چه چیزی
خراب بود» — که تصویر خوبی نمی‌سازد. متن‌ها بازنویسی شدند تا **نتیجه‌ی فعلی** را
بگویند نه نقص گذشته را. **هیچ موردی حذف نشد**؛ همه ویرایش‌ها جایگزینی یک‌به‌یک
متن بودند و شمارنده‌ی «تغییر کل» هر کارت دست‌نخورده ماند.

نسخه‌های بازنویسی‌شده: `2.15.0`, `2.14.0`, `2.12.0`, `2.11.0`, `2.10.0`,
`2.7.1`, `2.7.0`, `2.6.0`, `2.1.0`, `2.0.1`, `2.0.0`, `1.8.8`, `1.8.7`,
`1.8.6`, `1.8.5`, `1.4.3` — شامل `title` و `description` نسخه‌ها، نه فقط آیتم‌ها.

نمونه:

| قبل | بعد |
|---|---|
| قیمت‌ها به تومان بودند ولی با واحد ریال به گوگل اعلام می‌شدند و ۱۰ برابر کمتر نمایش داده می‌شد | اعلام قیمت‌ها به گوگل با واحد پول صحیح (تومان) — نمایش دقیق قیمت در نتایج جستجو |
| رفع باگ جستجو در موبایل — آدرس نادرست API باعث می‌شد همیشه «نتیجه‌ای یافت نشد» نمایش داده شود | نمایش صحیح نتایج جستجو در نسخه موبایل |
| رفع TypeError: this.container is null در Story — اضافه کردن cancelled flag | پایدارسازی ویجت استوری هنگام جابه‌جایی سریع بین صفحات |

جزئیات داخلی بی‌معنا برای کاربر هم ساده شد (`min-w-0`, `force-dynamic`,
`revalidate=60`, `unoptimized`, `JWT_SECRET`, نام فایل‌ها).

### ۳) برچسب دسته‌ی `fix`

در `CHANGE_CONFIG` از **«رفع باگ»** به **«اصلاح»** تغییر کرد. با بازنویسی متن‌ها
هنوز کنار هر مورد یک برچسب زرد «رفع باگ» می‌نشست و همان پیامی را می‌داد که قرار
بود حذف شود. برگرداندنش یک خط است.

---

# استقرار

## 9dm.ir — انجام شد ✅

```bash
node scripts/deploy/deploy.mjs 9dm --dry-run
node scripts/deploy/deploy.mjs 9dm
```

**نتیجه:** موفق. health check در اولین تلاش `HTTP 200`.

### بررسی‌های پیش از استقرار

- `next build` محلی موفق.
- dry-run: **۰ حذف**، بدون `public/uploads`، بدون `.env`.
- فقط سه فایل تغییر محتوایی داشتند (`<f.st` در خروجی rsync):
  `CategoryPageClient.tsx`, `FullBannerSection.tsx`, `changelog/page.tsx`.
  بقیه‌ی فایل‌ها فقط اختلاف زمان (`<f..t`) داشتند یعنی محتوایشان از قبل با سرور
  یکی بود.
- پوشه‌ی migration `20260811123716_add_product_identifiers` از قبل روی سرور بود،
  یعنی کارهای سئو (۲.۱۴/۲.۱۵) قبلاً مستقر شده بودند. این استقرار **فقط** کار
  امروز را برد.

### ⚠️ محدودیت اعتبارسنجی

`9dm.ir` عملاً خالی است — نقشه سایت فقط ۵ نشانی دارد و هیچ محصول یا دسته‌بندی
ندارد، و ویجت بنر تمام‌عرض هم روی صفحه اصلی‌اش تنظیم نشده.

پس روی 9dm فقط این‌ها تأیید شد:

- استقرار و build سالم است و سایت بالا می‌آید.
- کد قدیمی رفته: `aspect-[1774/426]` صفر بار در HTML خروجی.

اما **رفتار واقعی دو اصلاح روی 9dm قابل آزمایش نبود** چون داده‌ای ندارد.
آزمون واقعی روی مهام‌پرینت انجام شد (پایین‌تر).

> برای دفعات بعد: 9dm فقط برای سنجش سلامت build و استقرار مفید است. هر اصلاحی
> که به داده وابسته باشد باید روی مهام‌پرینت آزمایش شود.

## سایت‌های اصلی — انجام شد ✅

هر سه سایت پروداکشن با موفقیت مستقر شدند. health check هر سه در **اولین تلاش**
`HTTP 200` داد.

| سایت | پورت | نتیجه |
|---|---|---|
| mahamprint | 3002 | ✅ https://mahamprint.com |
| bartarjanebi | 3001 | ✅ https://bartar-janebi.com |
| mymonta | 3000 | ✅ https://mymonta.ir |

آماده‌سازی (dry-run و بکاپ‌گیری) توسط Claude انجام شد، ولی **اجرای نهایی
توسط طبقه‌بند امنیتی Claude Code مسدود شد** (اجرای اسکریپت استقرار پروداکشن با
فلگ `--yes`). دستورها دستی در ترمینال اجرا شدند:

```bash
node scripts/deploy/deploy.mjs mahamprint      # اول این
node scripts/deploy/deploy.mjs bartarjanebi
node scripts/deploy/deploy.mjs mymonta
```

هر اجرا دو پرامپت دارد: `yes` و سپس `backup-ok`.

> **برای دفعات بعد:** همین الگو را نگه دارید — Claude آماده‌سازی (dry-run،
> بکاپ روی لپ‌تاپ، صحت‌سنجی) را انجام می‌دهد و اجرای نهایی پروداکشن دستی است.

### dry-run هر سه سایت — انجام شد

| سایت | حذفی‌ها | `public/uploads` یا `.env` | فایل‌های تغییر محتوایی |
|---|---|---|---|
| mahamprint | ۰ | ندارد | ۳ |
| bartarjanebi | ۰ | ندارد | ۳ |
| mymonta | ۰ | ندارد | ۳ |

هر سه دقیقاً همان سه فایل امروز را می‌برند.

### بکاپ دیتابیس روی لپ‌تاپ — انجام شد

مسیر: `~/mymonta-backups/2026-08-12_1340/`

| سایت | فایل | حجم | رکورد محصول |
|---|---|---|---|
| mahamprint | `mahamprint.sql.gz` | ۴.۱ مگابایت | ۲۷۵۷ |
| bartarjanebi | `bartarjanebi.sql.gz` | ۹۲ کیلوبایت | ۱۶۱ |
| mymonta | `mymonta.sql.gz` | ۳۳۲ کیلوبایت | ۲۰ |

صحت هر سه بررسی شد — بلوک `COPY public."Product"` کامل است. پس پاسخ `backup-ok`
در پرامپت اسکریپت واقعاً درست است، نه دور زدن دروازه.

روش گرفتن بکاپ (پسورد از طریق stdin، نه خط فرمان، تا در لاگ ظاهر نشود):

```bash
printf 'set -o pipefail\nexport PGHOST=%s PGPORT=%s PGUSER=%s PGDATABASE=%s\nexport PGPASSWORD="$1"\npg_dump | gzip\n' \
  "$H" "$P" "$U" "$D" | ssh root@$host bash -s -- "$PASS" > mahamprint.sql.gz
```

### دروازه‌های ایمنی اسکریپت برای `tier: production`

1. تأیید `yes` برای شروع.
2. بکاپ دیتابیس با `pg_dump` روی سرور + توقف اگر حجم زیر ۱۰ کیلوبایت باشد.
3. توقف اگر بیش از ۱۰۰ محصول ولی بکاپ زیر ۵۰ کیلوبایت باشد.
4. تأیید دستی با تایپ `backup-ok`.
5. بکاپ کد فعلی در `/var/backups/mymonta/<name>-code-<ts>.tar.gz`.
6. `--max-delete=200` روی rsync.
7. health check و **بازگردانی خودکار** در صورت شکست.

`public/uploads`, `public/products`, `public/banners`, `public/brands`,
`public/categories`, `public/mid-banners` و `.env` همگی در `EXCLUDES` هستند و
هرگز منتقل یا حذف نمی‌شوند.

### اعتبارسنجی پس از استقرار مهام‌پرینت — انجام شد ✅

مهام‌پرینت تنها جایی بود که هر دو اصلاح با داده‌ی واقعی قابل آزمایش بودند.
آزمون چشمی روی سایت زنده انجام شد و **هر دو تأیید شدند**:

- بنر تمام‌عرض صفحه اصلی کامل و بدون برش نمایش داده می‌شود.
- اسکرول و صفحه‌بندی در صفحات دسته‌بندی درست کار می‌کند.

دستورهای بررسی خودکار برای دفعات بعد:

```bash
# دسته پرینتر باید بیش از ۱۲ محصول و لینک صفحه بعد داشته باشد
curl -s 'https://mahamprint.com/categories/printer' | grep -c 'نمایش محصولات بیشتر'
curl -s 'https://mahamprint.com/categories/printer?page=3' | grep -o 'page=[0-9]*' | sort -u

# بنر تمام‌عرض نباید نسبت ثابت داشته باشد
curl -s 'https://mahamprint.com/' | grep -c 'aspect-\[1774/426\]'   # باید ۰ باشد
curl -s 'https://mahamprint.com/' | grep -o 'class="w-full h-auto object-cover[^"]*"' | head -1
```

به‌علاوه چشمی: اسکرول در دسته پرینتر و کامل بودن بنر متحرک صفحه اصلی.

---

# فایل‌های تغییریافته

| فایل | تغییر |
|---|---|
| `components/store/categories/CategoryPageClient.tsx` | افکت observer، مرز صفحه‌بندی، لینک‌های قابل کراول، `pageWindow`, `hrefFor`, `history.replaceState`, حذف `useRouter` |
| `components/store/FullBannerSection.tsx` | حذف `aspect` ثابت و `fill`، بازگردانی کلاس‌های سایه و hover |
| `app/admin/changelog/page.tsx` | نسخه ۲.۱۶.۰ + بازنویسی ۱۶ نسخه + برچسب `fix` |
