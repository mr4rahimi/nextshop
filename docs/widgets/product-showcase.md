# تغییرات — کارت محصول جدید و ویجت «نمایش محصولات»

**تاریخ:** ۱۴۰۵/۰۵/۲۳ (2026-08-14)
**نسخه:** 2.19.0

یک کارت محصول تازه ساخته شد و یک ویجت جدید که از آن استفاده می‌کند. ویجت جدید
کار هر دو ویجت قبلی (محصولات بر اساس دسته / بر اساس برند) را در یک ویجت انجام
می‌دهد و منبع از ادمین انتخاب می‌شود. **هر دو ویجت قبلی دست‌نخورده باقی مانده‌اند.**

| # | موضوع | فایل اصلی |
|---|---|---|
| [۱](#۱--کارت-محصول-جدید) | کارت محصول جدید | `components/store/product/ShowcaseProductCard.tsx` |
| [۲](#۲--اندازهی-sm-برای-دکمهی-سبد-خرید) | اندازه‌ی `sm` دکمه‌ی سبد | `components/store/cart/AddToCartButton.tsx` |
| [۳](#۳--ویجت-نمایش-محصولات) | ویجت نمایش محصولات | `components/store/ProductShowcaseSection.tsx` |
| [۴](#۴--پنل-ادمین) | پنل ادمین | `app/admin/widgets/[id]/page.tsx` |
| [۵](#۵--مشکلاتی-که-حین-تست-پیدا-و-برطرف-شد) | مشکلات پیداشده حین تست | — |

---

## ۱ — کارت محصول جدید

`components/store/product/ShowcaseProductCard.tsx`

کارتی که برای **دو تایی نشستن در موبایل** طراحی شده. عمداً مستقل از ویجت نوشته
شده تا بعداً در صفحه‌ی همه محصولات، دسته‌بندی، برند و هر جای دیگری استفاده شود:

```tsx
<ShowcaseProductCard product={p} showBrand fallbackBrandTitle={brandTitle} />
```

### محتوای کارت

- تصویر مربع تمام‌عرض (نسبت ثابت ۱:۱)
- بَج درصد تخفیف بالا-راست، دکمه‌ی علاقه‌مندی بالا-چپ (در موبایل همیشه دیده می‌شود،
  برخلاف کارت قبلی که فقط با hover ظاهر می‌شد و در لمسی عملاً در دسترس نبود)
- برچسب برند، عنوان، قیمت با خط‌خوردگی قیمت قبلی، و دکمه‌ی گرد «+» سبد خرید
- حالت ناموجود با بَج و متن اختصاصی

### چرا در یک ردیف همه هم‌قد و هم‌تراز می‌شوند

سه ارتفاع عمداً ثابت شده‌اند:

| بخش | روش |
|---|---|
| تصویر | `aspect-square` |
| عنوان | `line-clamp-2` + ارتفاع ثابت (`h-[2.5rem]` موبایل، `sm:h-[2.75rem]`) |
| برچسب برند | ظرف با ارتفاع ثابت `h-4.5` که حتی بدون برند هم جا می‌گیرد |
| قیمت خط‌خورده | `<span>` با `h-4` که حتی بدون تخفیف هم جا می‌گیرد |

بدون این‌ها، کارت‌های بدون برند یا بدون تخفیف عنوان و قیمتشان بالاتر می‌افتاد و
ردیف نامرتب می‌شد (این مورد در تست دسکتاپ دیده و اصلاح شد).

### اندازه در موبایل

در عرض ۳۹۰ پیکسل هر کارت **۱۸۴ در ۳۲۳ پیکسل** اندازه‌گیری شد؛ یعنی دو کارت
(۱۸۴×۲ + ۱۲ فاصله = ۳۸۰) داخل ظرف جا می‌شود و کارت بیش از حد دراز نیست.

---

## ۲ — اندازه‌ی `sm` برای دکمه‌ی سبد خرید

دکمه‌ی پیش‌فرض `AddToCartButton` در حالت آیکن ۵۶×۵۶ پیکسل است و حالت شمارنده‌اش
(وقتی محصول در سبد است) حدود ۷۶ پیکسل عرض می‌گیرد — در کارت فشرده‌ی موبایل
چیدمان را می‌شکست، مخصوصاً چون کلاس دلخواه فقط روی حالت «+» اثر داشت و شمارنده
کلاس‌هایش hard-code بود.

یک prop اختیاری `size?: "sm" | "md"` اضافه شد که **هر دو حالت** را کوچک می‌کند.
پیش‌فرض `md` است، پس همه‌ی استفاده‌های قبلی دقیقاً مثل قبل کار می‌کنند.

هم‌زمان `aria-label` به دکمه‌های افزودن، افزایش و کاهش تعداد اضافه شد (قبلاً
دکمه‌ها برای صفحه‌خوان بدون نام بودند).

---

## ۳ — ویجت «نمایش محصولات»

`components/store/ProductShowcaseSection.tsx` — نوع `PRODUCT_SHOWCASE`

### config

| کلید | معنی |
|---|---|
| `source` | `"category"` یا `"brand"` — منبع محصولات |
| `categoryId` / `categoryTitle` / `categorySlug` | وقتی منبع دسته است |
| `brandId` / `brandTitle` / `brandSlug` | وقتی منبع برند است |
| `count` | تعداد محصول (۱ تا ۲۰) |
| `sortMode` | `newest` / `best_sellers` / `manual` |
| `productIds` | فقط در حالت `manual` |
| `heading` | عنوان جایگزین؛ خالی یعنی نام دسته/برند |
| `showBrandBadge` | نمایش برچسب برند روی کارت |
| `showArrows` | نمایش فلش‌های ناوبری |
| `autoplay` / `autoplayDelay` | اسلاید خودکار و فاصله‌اش (۱ تا ۱۵ ثانیه) |

`normalizeShowcaseConfig()` مقادیر را امن می‌کند و هم در رندر و هم در ادمین صدا
زده می‌شود.

### رفتار اسلایدر

- **موبایل:** `slidesPerView: 2.25` — دو کارت کامل + بخشی از کارت سوم، تا معلوم
  باشد لیست ادامه دارد. در ۴۸۰px به ۲٫۴، در ۶۴۰px به ۳٫۲، در ۱۰۲۴px به ۴٫۲ و در
  ۱۲۸۰px به ۵٫۲ می‌رسد.
- **کشیدن با انگشت** همیشه فعال است، حتی وقتی فلش‌ها خاموش‌اند.
- **اسلاید خودکار** با `pauseOnMouseEnter` و `disableOnInteraction: false`؛ وقتی
  بیش از ۴ محصول باشد حلقه‌ای می‌شود تا به انتها نرسد و متوقف نماند.
- **سربرگ:** عنوان و تعداد در راست؛ «مشاهده همه» و دو فلش در چپ. «مشاهده همه» به
  `/categories/<slug>` یا `/brands/<slug>` می‌رود.

### API

از همان دو endpoint موجود استفاده می‌شود و **هیچ API جدیدی ساخته نشد**:
`/api/store/products-by-category` و `/api/store/products-by-brand` — هر دو از قبل
سه حالت `productIds`، `sort=newest` و `sort=best_sellers` را پشتیبانی می‌کردند.

نکته: endpoint برند فیلد `brand` را برنمی‌گرداند (چون برند از قبل معلوم است)، پس
نام برند از config به کارت پاس داده می‌شود (`fallbackBrandTitle`).

---

## ۴ — پنل ادمین

`ProductShowcaseEditor` سه بخش دارد:

۱. **منبع محصولات** — دو کارت انتخابی: دسته‌بندی یا برند.
۲. **انتخابگر** — بسته به منبع، **دقیقاً همان** `ProductsByCategoryEditor` یا
   `ProductsByBrandEditor` ویجت‌های قبلی رندر می‌شود (جستجو، انتخاب تعداد، سه حالت
   مرتب‌سازی و انتخاب دستی محصولات). هیچ کدی کپی نشد.
۳. **نمایش و حرکت اسلایدر** — عنوان جایگزین، سه سوییچ (فلش‌ها، برچسب برند، اسلاید
   خودکار) و اسلایدر زمان‌بندی که فقط با روشن بودن اسلاید خودکار ظاهر می‌شود.

هنگام ذخیره، فیلدهای منبعِ غیرفعال پاک می‌شوند تا config تمیز بماند و اگر حالت
`manual` نباشد `productIds` خالی می‌شود.

---

## ۵ — مشکلاتی که حین تست پیدا و برطرف شد

### الف) hydration mismatch به‌خاطر کلاس یکتا

روش ویجت‌های قبلی برای ساخت کلاس یکتای سوایپر
(`useRef(\`pbc-${Math.random()}\`)`) روی سرور و کلاینت دو مقدار متفاوت تولید
می‌کند و React خطای hydration می‌دهد. جایگزینی با `useId` هم جواب نداد، چون زیر
`next/dynamic` جایگاه کامپوننت در درخت بین SSR و hydration فرق می‌کند و id متفاوت
درمی‌آید (`:R454...:` در برابر `:R54...:`).

راه‌حل: **اصلاً کلاس یکتا ساخته نمی‌شود.** خود المان‌ها با `ref` به سوایپر داده
می‌شوند:

```tsx
const rootRef = useRef<HTMLDivElement>(null);
new win.Swiper(rootRef.current, {
  navigation: { nextEl: nextRef.current, prevEl: prevRef.current },
});
```

این روش هم SSR-safe است و هم چند نمونه‌ی هم‌زمان از ویجت را بدون تداخل پشتیبانی
می‌کند.

> دو ویجت قدیمی هنوز روش `Math.random()` را دارند و همین خطا را تولید می‌کنند.
> چون قرار بود دست‌نخورده بمانند تغییری در آن‌ها داده نشد، اما اگر بعداً خواستید
> اصلاح شود، الگوی بالا آماده است.

### ب) aria-label انگلیسی روی فلش‌ها

ماژول a11y سوایپر `aria-label` دکمه‌های ناوبری را بازنویسی می‌کند و پیش‌فرضش
انگلیسی است؛ در تست، دکمه‌ها با «Previous slide» و «Next slide» ظاهر شدند. با ست
کردن `a11y: { prevSlideMessage, nextSlideMessage, ... }` متن‌ها فارسی شدند.

### ج) نامرتب بودن عنوان‌ها در یک ردیف

چون برچسب برند شرطی بود، عنوان کارت‌های بدون برند بالاتر می‌افتاد. ظرف برچسب حالا
ارتفاع ثابت دارد (بخش ۱).

### د) شکستن چیدمان با دکمه‌ی سبد خرید

توضیح در بخش ۲.

---

## تست انجام‌شده

`tsc --noEmit` بدون خطا. تست‌ها با مرورگر واقعی روی سرور توسعه:

| سناریو | نتیجه |
|---|---|
| منبع دسته‌بندی، موبایل ۳۹۰px | ۲ کارت کامل + بخشی از سومی، کارت ۱۸۴×۳۲۳ ✓ |
| منبع دسته‌بندی، دسکتاپ ۱۴۴۰px | ۵٫۲ کارت، چیدمان مرتب ✓ |
| منبع برند | عنوان «HP»، «مشاهده همه» → `/brands/hp` ✓ |
| `showArrows: false` | هیچ فلشی رندر نشد، کشیدن با انگشت فعال ماند ✓ |
| `showArrows: true` | کلیک روی فلش اسلاید را جابه‌جا کرد (activeIndex از ۰ به ۱) ✓ |
| `autoplay: true` | `swiper.autoplay.running === true` ✓ |
| خطای hydration | پس از اصلاح، کنسول تمیز شد ✓ |
| aria-label فلش‌ها | «اسلاید قبلی» / «اسلاید بعدی» ✓ |

> نکته‌ی محیطی: تصاویر محصولات این دیتابیس روی `mahamprint.com` میزبانی می‌شوند و
> این محیط به اینترنت دسترسی ندارد؛ برای بررسی بصری، تصاویر فقط در مرورگرِ تست با
> فایل‌های محلی جایگزین شدند (بدون تغییر در دیتابیس).

---

## فایل‌های تغییریافته

```
components/store/product/ShowcaseProductCard.tsx  ← جدید (کارت محصول)
components/store/ProductShowcaseSection.tsx       ← جدید (ویجت)
components/store/cart/AddToCartButton.tsx         ← prop اختیاری size + aria-label
components/store/StoreHomePageClient.tsx          ← import + case
app/admin/widgets/page.tsx                        ← WIDGET_META + یونیون نوع
app/admin/widgets/[id]/page.tsx                   ← ProductShowcaseEditor + اتصالات
prisma/schema.prisma                              ← PRODUCT_SHOWCASE در enum
prisma/migrations/20260814103306_add_product_showcase_widget/
```

## استفاده‌ی بعدی از کارت جدید

برای نمایش دوتایی محصولات در موبایل در صفحات دیگر (همه محصولات، دسته‌بندی، برند)
کافی است `ShowcaseProductCard` داخل یک گرید قرار گیرد:

```tsx
<div className="grid grid-cols-2 gap-3 md:grid-cols-3 lg:grid-cols-4">
  {products.map(p => <ShowcaseProductCard key={p.id} product={p} />)}
</div>
```

`sizes` را متناسب با گرید مقصد پاس دهید تا تصاویر با اندازه‌ی درست دانلود شوند.
