# تغییرات سئو — صفحه محصول و فنی

**تاریخ:** ۱۴۰۵/۰۵/۲۰ (2026-08-11)

این فایل دو بخش دارد که هر دو در یک روز انجام شدند:

| بخش | نسخه | دامنه |
|---|---|---|
| [بخش اول — سئوی صفحه محصول](#بخش-اول--سئوی-صفحه-محصول) | 2.14.0 | صفحه تکی محصول (`/products/[slug]`) و ساختار داده‌ی آن |
| [بخش دوم — Technical SEO](#بخش-دوم--technical-seo-همان-روز) | 2.15.0 | کل سایت: هدرها، ایندکس‌پذیری، sitemap، robots، LCP، آپلود، IndexNow |
| [بخش سوم — استقرار و گواهی HTTPS](#بخش-سوم--استقرار-و-گواهی-https) | — | استقرار روی ۴ سایت، اصلاح گواهی‌ها، خروج از CDN |

> **مهم:** همه‌ی اصلاحات هر دو بخش سطح **کد** هستند و روی هر ۵ دیپلوی مشترک‌اند.
> مواردی که به داده‌ی هر سایت وابسته‌اند عمداً انجام نشده‌اند و در انتهای فایل
> فهرست شده‌اند.

---

# بخش اول — سئوی صفحه محصول

**نسخه:** 2.14.0

## چطور به این تغییرات رسیدیم

اسکیل `claude-seo` نصب شد و با دستور `/seo ecommerce` یک آدیت e-commerce روی
دو صفحه محصول واقعی روی لوکال اجرا شد:

- `/products/----hp-pro-mfp-m28a` (محصول کم‌داده: بدون تصویر، بدون برند)
- `/products/----hp-mfp-m183fw` (محصول کامل: با تصویر، برند و توضیحات)

هر دو صفحه با Chromium رندر شدند (نه فقط SSR) و خروجی با `parse_html.py` تحلیل شد،
به‌علاوه چند کوئری مستقیم روی دیتابیس برای سنجش پوشش داده در کل ۲۷۴۲ محصول فعال.

**امتیاز اولیه: ۴۹/۱۰۰**

> نکته: ابزار `render_page.py` خودِ اسکیل به‌خاطر محافظ SSRF روی `localhost`
> کار نمی‌کند و خطای `Blocked hostname: localhost` می‌دهد. راه دور زدنش این است
> که رندر مستقیم با Chromium خودِ اسکیل گرفته شود و فایل HTML به `parse_html.py`
> داده شود.

---

## ۱. باگ واحد پول — قیمت ۱۰ برابر کمتر به گوگل اعلام می‌شد

### مشکل
قیمت‌ها در دیتابیس و رابط کاربری **تومان** هستند، ولی `buildProductSchema`
مقدار خام را با `priceCurrency: "IRR"` منتشر می‌کرد:

```
UI:     ۸۷٬۰۰۰٬۰۰۰ تومان
Schema: "price": 87000000, "priceCurrency": "IRR"   ← یعنی ۸٬۷۰۰٬۰۰۰ تومان
```

این در Google Merchant Center به‌عنوان **price mismatch** شناخته می‌شود و
می‌تواند به تعلیق فید منجر شود؛ در rich result هم قیمت غلط نمایش می‌داد.

### چرا با تغییر `priceCurrency` حل نمی‌شود
تومان کد ISO 4217 ندارد و Schema.org فقط کد ISO می‌پذیرد. پس تنها راه درست،
تبدیل عدد به ریال است (۱ تومان = ۱۰ ریال).

### تغییر — `lib/seo.ts`
```ts
export const SCHEMA_CURRENCY = "IRR";
const TOMAN_TO_IRR = 10;
export function tomanToIrr(n: number) { return Math.round(n * TOMAN_TO_IRR); }
```

اعمال شد روی:
- `offers.price`
- `offers.shippingDetails.shippingRate.currency`
- هر دو `UnitPriceSpecification` (قیمت اصلی و قیمت تخفیف‌خورده)

پارامتر `currency` از ورودی تابع حذف شد تا کسی نتواند دوباره واحد ناسازگار بدهد.

---

## ۲. شناسه‌های محصول (SKU / GTIN-13 / MPN)

### مشکل
`app/(shop)/products/[slug]/page.tsx` خط `sku: product.sku` را داشت، ولی مدل
`Product` اصلاً ستون `sku` نداشت. (آن `sku`هایی که در `schema.prisma` بود مال
`AssembledProduct` و `IntegPlatformProduct` است.) پس این خط همیشه بی‌صدا
`undefined` می‌داد و `sku` هرگز در JSON-LD نمی‌آمد.

### تغییرات

**`prisma/schema.prisma`** — سه فیلد به `Product`:
```prisma
sku    String?
gtin13 String?
mpn    String?
```
Migration: `20260811123716_add_product_identifiers`

**`lib/seo.ts`** — `buildProductSchema` حالا `gtin13` و `mpn` را هم می‌پذیرد و منتشر می‌کند.

**`app/api/admin/products/route.ts`** و **`app/api/admin/products/[id]/route.ts`**
— سه فیلد به ساخت و ویرایش اضافه شدند.

**`app/api/admin/products/duplicate/route.ts`** — عمداً `null` می‌شوند.
شناسه‌ی تکراری بدتر از نبودِ شناسه است؛ هر محصول باید SKU مخصوص خودش را داشته باشد.

**`components/admin/product/ProductForm.tsx`** — بخش «شناسه‌های محصول» زیر تب SEO
با سه ورودی. `FormState` و `EMPTY_FORM` هم به‌روز شدند.

**`app/admin/products/[id]/page.tsx`** — hydration فرم از محصول موجود.

> ⚠️ بعد از `prisma migrate dev` حتماً `prisma generate` بزنید **و سرور dev را
> ری‌استارت کنید**. سرور در حال اجرا Prisma client قدیمی را در حافظه نگه می‌دارد
> و فیلدهای جدید بی‌صدا `null` برمی‌گردند — این دقیقاً وسط همین کار پیش آمد.

---

## ۳. محتوای تب‌ها برای گوگل نامرئی بود ← مهم‌ترین دستاورد

### مشکل
`ProductDetailClient.tsx` پنل‌های تب را با شرط رندر می‌کرد:

```tsx
{activeTab === "expert" && ( ... )}
```

یعنی فقط تب فعال (`specs`) در DOM بود. **بررسی اجمالی، بررسی تخصصی، نظرات و
پرسش‌وپاسخ اصلاً در HTML وجود نداشتند** و گوگل هیچ‌وقت آن‌ها را نمی‌دید.
به‌علاوه `FAQPage` schema منتشر می‌شد در حالی که متن پاسخ‌ها روی صفحه نبود —
که طبق قوانین گوگل schema را نامعتبر می‌کند.

### تغییر
کامپوننت `TabPanel` اضافه شد: محتوا همیشه رندر می‌شود و فقط با کلاس `hidden`
(یعنی `display:none`) پنهان می‌شود. گوگل محتوای پنهان‌شده با CSS را ایندکس می‌کند،
ولی محتوایی که در DOM نیست را قطعاً نمی‌بیند.

پاسخ‌های آکاردئون FAQ هم از حالت شرطی به `hidden` تغییر کردند.

### نتیجه
```
حجم محتوای قابل ایندکس هر صفحه محصول:
۳۲۲ کلمه → ۱۰۲۲ کلمه
```

---

## ۴. ساختار عناوین (H2)

### مشکل
صفحات محصول **صفر تا H2** داشتند. تنها `<h2>` موجود داخل `RelatedSection` بود
که چون جدول `ProductRelated` صفر رکورد دارد و `relatedSettings` خاموش است،
هرگز رندر نمی‌شد. بخش‌های مشخصات/نظرات/معرفی همه با `div` و `h3` ساخته شده بودند.

### تغییر
- هر `TabPanel` یک `<h2 className="sr-only">` با عنوان معنادار می‌گیرد
  (مثل «مشخصات فنی {نام محصول}») — پنج H2 در مجموع.
- عنوان گروه مشخصات از `h4` به `h3` تغییر کرد (سلسله‌مراتب درست زیر H2).
- بج‌های اعتماد («ارسال فوری»، «پرداخت امن») از `h3` به `p` تبدیل شدند —
  عنوان بخش نیستند.
- عدد امتیاز کلی از `h4` به `span` تبدیل شد — یک عدد است، نه عنوان.

---

## ۵. مشخصات فنی به‌صورت جدول واقعی

`ProductSpecValue` حدود ۲۵۰۰۰ رکورد دارد ولی با `div` رندر می‌شد.
حالا `<table>` با `<th scope="row">` برای نام ویژگی و `<td>` برای مقدار،
به‌همراه `<caption className="sr-only">`. ظاهر با Tailwind دست‌نخورده ماند
(`grid` روی `tbody`).

---

## ۶. `og:type` برای صفحه محصول

### مشکل
همه صفحات `og:type: website` می‌دادند، از جمله صفحات محصول.

### چرا ساده نبود
دو راه بدیهی هر دو شکست خوردند:

1. **`openGraph.type = "product"`** → Next در زمان اجرا خطا می‌دهد:
   `⨯ Error: Invalid OpenGraph type: product` و صفحه با ۵۰۰ می‌شکند.
   حتی با cast تایپ‌اسکریپت، اعتبارسنجی runtime جلویش را می‌گیرد.
2. **`other: { "og:type": "product" }`** → Next آن را با `name=` می‌سازد
   نه `property=`. پارسرهای Open Graph فقط `property` را می‌خوانند.

### راه‌حل
`buildBaseMetadata` وقتی `ogType === "product"` است عمداً `type` را ست نمی‌کند
(تا تگ تکراری تولید نشود)، و خودِ صفحه محصول تگ را با JSX می‌سازد:

```tsx
<meta property="og:type" content="product" />
```

React 19 آن را خودکار به `<head>` منتقل می‌کند.

---

## ۷. بقیه اصلاحات

| مورد | فایل | تغییر |
|---|---|---|
| `priceValidUntil` در هر ریکوئست عوض می‌شد (`now + ۷ روز`) و خروجی JSON-LD را غیرقطعی می‌کرد | `lib/seo.ts` | تاریخ ثابت: پایان سال میلادی بعدی. قابل override با پارامتر |
| اسلاگ فارسی دسته‌بندی encode-نشده در `BreadcrumbList.item` (مثل `/categories/شیرالات-دوم`) که URL نامعتبر است | `lib/seo.ts` + صفحه محصول | تابع `encodeSlug()` — اگر از قبل encode شده باشد دوباره encode نمی‌کند |
| ستون `seoSchema` روی `Product` وجود داشت، API برمی‌گرداندش، ولی هیچ‌وقت رندر نمی‌شد | صفحه محصول | `parseCustomSchema()` تزریقش می‌کند؛ JSON نامعتبر بی‌صدا رد می‌شود تا صفحه نشکند |
| meta description بدون قیمت و CTA | صفحه محصول | `buildProductDescription()` قیمت و CTA اضافه می‌کند، **فقط** اگر مجموع زیر ۱۵۵ کاراکتر بماند |

---

## تأیید

- `tsc --noEmit` تمیز.
- هر دو صفحه محصول با Chromium رندر و با `parse_html.py` بررسی شدند:
  - `"price": 870000000, "priceCurrency": "IRR"` ✓
  - `sku` / `gtin13` / `mpn` با داده‌ی تستی در JSON-LD ظاهر شدند ✓
  - `priceValidUntil: "2027-12-31"` ثابت ✓
  - breadcrumb دسته‌ی فارسی → `%D8%B4%DB%8C%D8%B1...` ✓
  - `<meta property="og:type" content="product">` تنها یک بار ✓
  - ۵ تا H2، یک `<table>`، ۵ تا `role="tabpanel"` ✓
- تزریق `seoSchema` سفارشی تست شد (`VideoObject` کنار `Product` و `BreadcrumbList` ظاهر شد).
- `seoSchema` با JSON خراب تست شد → صفحه ۲۰۰ ماند.
- تمام داده‌ی تستی از دیتابیس پاک شد.

---

# بخش دوم — Technical SEO (همان روز)

**نسخه:** 2.15.0 · اجرا با `/seo technical`

آدیت فنی روی همان محیط لوکال اجرا شد. برای اینکه اعداد عملکرد واقعی باشند
`pnpm build` گرفته شد و سرور production روی پورت ۳۱۰۰ اندازه‌گیری شد —
اندازه‌گیری روی dev server گمراه‌کننده است.

**امتیاز اولیه: ۶۲/۱۰۰**

| دسته | امتیاز اولیه |
|---|---|
| Crawlability | 85 |
| Indexability | 62 |
| Security | 45 |
| URL Structure | 65 |
| Mobile | 62 |
| Core Web Vitals | 45 |
| Structured Data | 88 |
| JS Rendering | 95 |
| IndexNow | 0 |

همه‌ی اصلاحات این بخش **سطح کد** هستند و روی هر ۵ دیپلوی مشترک‌اند.

## ۸. هدرهای امنیتی و افشای نسخه — `next.config.ts`

`headers()` فقط `Cache-Control` داشت. اضافه شد روی `/:path*`:
`Strict-Transport-Security`، `X-Content-Type-Options`، `X-Frame-Options`،
`Referrer-Policy`، `Permissions-Policy`، `X-DNS-Prefetch-Control`.
به‌علاوه `poweredByHeader: false` (هدر `X-Powered-By: Next.js` حذف شد).

**عمداً CSP اضافه نشد.** Next اسکریپت inline تزریق می‌کند (اسکریپت ضد-flash تم،
هیدریشن، JSON-LD) و یک CSP نادرست همزمان چند سایت production را می‌شکند.
اگر خواستید، اول با `Content-Security-Policy-Report-Only` روی یک سایت تست کنید.

`HSTS` عمداً بدون `includeSubDomains` و `preload` است تا اگر ساب‌دامنه‌ای هنوز
HTTPS ندارد از کار نیفتد. روی HTTP لوکال هم مرورگر نادیده‌اش می‌گیرد، پس در
توسعه بی‌اثر و بی‌خطر است.

`dangerouslyAllowSVG` حذف شد — فقط روی بهینه‌ساز اثر داشت که با
`unoptimized: true` بای‌پس شده، پس صرفاً ریسک خفته بود.

## ۹. صفحه‌بندی به صفحه ۱ کنونیکال می‌شد

بزرگ‌ترین ایراد ایندکس‌پذیری. در `categories/[slug]/page.tsx` شرط
`page > 1` با فیلترها یک‌کاسه شده بود:

```ts
if (hasFilters || hasSort || hasPrice || page > 1) { noIndex: true, canonicalPath: basePath }
```

برای فیلتر درست است، برای صفحه‌بندی نه. صفحه ۲ محتوای تکراری نیست و کنونیکال
کردنش به صفحه ۱ باعث می‌شود محصولاتی که فقط در صفحات بعدی هستند درست کشف نشوند.
گوگل صراحتاً این کار را منع می‌کند.

## ۱۰. سیاست ایندکس مشترک — `lib/catalog.ts`

`/categories/x?brand=y` نوایندکس می‌شد ولی `/products?brand=y` نه، چون
`/products` اصلاً `metadata` ثابت داشت و `searchParams` را نمی‌دید.

توابع `analyzeListingParams()` و `listingIndexPolicy()` اضافه شدند و هر دو صفحه
از یک منبع واحد استفاده می‌کنند. سه حالت:

| حالت | robots | canonical |
|---|---|---|
| فیلتر / مرتب‌سازی / قیمت | `noindex, follow` | صفحه پایه |
| صفحه‌بندی خالص | `index, follow` | خودارجاع (`?page=N`) |
| صفحه پایه | `index, follow` | خودش |

## ۱۱. robots.txt و صفحات خصوصی

گروه جداگانه‌ی `Googlebot` حذف شد. آن گروه `/search` را disallow نمی‌کرد و چون
در robots.txt گروه اختصاصی‌تر برنده است، قانون گروه `*` برای گوگل بی‌اثر بود.

`/cart`، `/checkout` و `/search` **از disallow خارج شدند** و به‌جایش
`noindex, follow` گرفتند. منطق: این صفحات از هدر به همه‌جا لینک دارند؛ اگر
خزیدنشان بسته باشد گوگل تگ noindex را نمی‌بیند و ممکن است URL خالی را ایندکس کند.
اجازه‌ی خزیدن + noindex تنها راهی است که حذف قطعی را تضمین می‌کند.

اعمال از طریق لایوت تا کاسکید شود: `app/user/layout.tsx`، `app/auth/layout.tsx`،
`app/seller/layout.tsx` و `app/(shop)/checkout/layout.tsx` (جدید — به هر ۵ صفحه
checkout می‌رسد). تابع `noIndexMetadata()` در `lib/seo.ts`.

## ۱۲. sitemap

- همه‌ی اسلاگ‌ها با `encodeSlug()` انکد می‌شوند. دو آدرس فارسی
  (`شیرالات-دوم` و `ش`) به‌صورت خام منتشر می‌شدند که طبق استاندارد sitemap
  نامعتبر است و گوگل ردشان می‌کند.
- `lastModified` صفحات لیستی دیگر `now` نیست؛ از جدیدترین محتوای همان لیست
  می‌آید. `now` همیشگی یعنی به گوگل می‌گوییم صفحه هر بار عوض شده و سیگنال
  کاملاً بی‌ارزش می‌شود.

## ۱۳. LCP

اندازه‌گیری روی build production، ویوپورت موبایل ۳۹۰px:

| صفحه | LCP | عنصر |
|---|---|---|
| خانه | ۷۲۸ms | یک `<p>` — سالم |
| محصول | ۲۲۴۸ms | تصویر دامنه خارجی |
| دسته‌بندی | ۷۲۴۰ms (کش سرد) / ۲۱۴۴ms (گرم) | تصویر دامنه خارجی |

CLS در هر سه صفحه صفر بود. صفحه اصلی ۱۳۰ ریکوئست و ۳.۴MB تصویر داشت.

روی localhost تأخیر شبکه صفر است، پس تنها تأخیر باقی‌مانده رفت‌وبرگشت به دامنه
خارجی است — یعنی LCP عملاً کامل در گروِ آن دامنه است. اولین بازدید هر کاربر
همان حالت کش سرد است و همان عددی است که در CrUX ثبت می‌شود.

دو اصلاح کد:
- `fetchPriority="high"` روی تصویر hero محصول. با `unoptimized: true`، پراپ
  `priority` در next/image فقط `<link rel="preload">` می‌سازد و صفت را روی خود
  تگ نمی‌گذارد.
- `externalImageOrigins()` در `lib/seo.ts` + `<link rel="preconnect">` در صفحه
  محصول و دسته‌بندی. در صفحات لیستی تصاویر کارت‌ها preload نمی‌شوند و مرورگر تا
  دیدن تگ `<img>` سراغ DNS/TLS دامنه خارجی نمی‌رود.

## ۱۴. کوچک‌سازی تصاویر در آپلود — `app/api/admin/upload/route.ts`

صفحه اصلی ۳.۴MB تصویر دانلود می‌کرد و آیکون‌های ۲۴ پیکسلی فایل ۸۰۰×۸۰۰ بودند
(تا ۱۱ برابر بزرگ‌تر از نیاز)، چون با `unoptimized: true` هیچ resize خودکاری
در زمان نمایش انجام نمی‌شود.

حالا تصاویر آپلودی به حداکثر **۱۶۰۰ پیکسل** بزرگ‌ترین ضلع کوچک می‌شوند.

`sharp` به‌صورت **dynamic import داخل try/catch** صدا زده می‌شود و هر خطایی
نادیده گرفته می‌شود — روی سرورهایی که CPU از SSE4.2 پشتیبانی نمی‌کند (همان
دلیلی که `unoptimized: true` را لازم کرده) فایل اصلی بدون تغییر ذخیره می‌شود.
پس این تابع هیچ‌وقت آپلود را نمی‌شکند، فقط وقتی می‌تواند بهتر می‌کند.
SVG و GIF دست‌نخورده می‌مانند (برداری / متحرک).

منطق با سه حالت تست شد:

```
افقی  3000×2000 → 1600×1067   (35KB → 10KB)
عمودی  800×2400 →  533×1600
کوچک   400×300  →  400×300    (باید دست‌نخورده بماند)
```

## ۱۵. IndexNow — `lib/indexnow.ts` (جدید)

گوگل پشتیبانی نمی‌کند، ولی ایندکس Bing همان چیزی است که Microsoft Copilot از آن
نقل‌قول می‌کند، پس برای دیده‌شدن در پاسخ‌های AI ارزش دارد. هنگام ساخت، ویرایش و
حذف محصول آدرس اعلام می‌شود (بدون `await` تا پاسخ ادمین معطل نماند؛ هرگز
throw نمی‌کند و ذخیره محصول را نمی‌شکند).

فایل تأیید: `app/indexnow-key.txt/route.ts`. عمداً بیرون از `/api/` چون
robots.txt آن را می‌بندد.

**راه‌اندازی روی هر سایت:**

```bash
openssl rand -hex 16        # خروجی را در .env همان سایت بگذارید
INDEXNOW_KEY=<کلید>
```

بدون این متغیر همه‌چیز بی‌صدا غیرفعال است و مسیر کلید ۴۰۴ می‌دهد — هیچ سایتی
به‌خاطر نداشتنش خطا نمی‌دهد.

## ۱۶. دسترسی‌پذیری

سه عنصر تعاملی بدون نام قابل‌دسترس بودند: ورودی جستجو (`SearchBox.tsx`) و
آیکون‌های علاقه‌مندی و سبد خرید. `aria-label` گرفتند.

## تأیید بخش دوم

- `tsc --noEmit` تمیز، `pnpm build` موفق.
- هدرهای امنیتی در پاسخ دیده شدند، `X-Powered-By` حذف شد.
- سیاست ایندکس روی ۱۲ ترکیب آدرس تست شد (خانه، لیست، صفحه‌بندی، فیلتر،
  دسته‌بندی، مجله، سبد، تسویه، ورود، جستجو) — همه مطابق جدول بالا.
- sitemap: ۳۰۱۲ آدرس، صفر آدرس انکدنشده، `lastmod` برابر با
  `max(updatedAt)` دیتابیس.
- `<link rel="preconnect">` و `fetchPriority="high"` در HTML خروجی تأیید شدند.
- `/indexnow-key.txt` بدون کلید ۴۰۴ می‌دهد.

## آنچه در بخش دوم از قبل درست بود

**JS Rendering — ۹۵/۱۰۰.** SSR کامل است؛ canonical، meta robots، عنوان،
توضیحات و کل JSON-LD در HTML اولیه هستند نه تزریق‌شده با JS. دقیقاً همان چیزی
که مستندات دسامبر ۲۰۲۵ گوگل توصیه می‌کند و اکثر فروشگاه‌های Next اشتباه
انجام می‌دهند.

**CLS صفر** در هر سه نوع صفحه. `<html lang="fa" dir="rtl">`، viewport درست،
بدون اسکرول افقی، فونت پایه ۱۶px، ریدایرکت trailing slash با ۳۰۸ تک‌مرحله‌ای،
صفر `<div onclick>` و لندمارک‌های semantic کامل (`header`/`nav`/`main`/`footer`)
— یعنی برای agentهای هوش مصنوعی هم خوانا است. حجم HTML هر صفحه ۲۰–۳۰KB فشرده،
بسیار زیر سقف ۲MB گوگل‌بات. hreflang درست است که وجود ندارد (سایت تک‌زبانه).

---

# بخش سوم — استقرار و گواهی HTTPS

استقرار با `scripts/deploy/deploy.mjs` روی هر ۴ سایت انجام شد. ترتیب: اول دمو،
بعد تأیید، بعد سه سایت اصلی از کوچک به بزرگ.

## ترتیب و نتیجه

| سایت | محصولات | migration | health |
|---|---|---|---|
| 9dm.ir (دمو) | ۰ | ✓ | HTTP 200، تلاش ۱ |
| bartar-janebi.com | ۱۶۱ | ✓ | HTTP 200، تلاش ۱ |
| mahamprint.com | ۲۷۵۷ | ✓ | HTTP 200، تلاش ۱ |
| mymonta.ir | ۲۰ | ✓ | HTTP 200، تلاش ۱ |

هیچ‌کدام rollback نشدند. پروسه‌های نامرتبط روی سرورها دست نخوردند.

**تبدیل قیمت روی هر سه سایت اصلی تأیید شد:**

```
bartar-janebi   9000000 IRR ↔ ۹۰۰٬۰۰۰ تومان
mahamprint    125000000 IRR ↔ ۱۲٬۵۰۰٬۰۰۰ تومان
mymonta        51420000 IRR ↔ ۵٬۱۴۲٬۰۰۰ تومان
```

## بکاپ‌ها

قبل از هر استقرار، بکاپ دیتابیس روی **لپ‌تاپ** آورده شد (اسکریپت فقط روی سرور
می‌گذارد). محل: `~/mymonta-backups/*-PRE-SEO-*.sql.gz`. هر سه بایت‌به‌بایت با
سرور یکسان و gzip سالم (۷۶–۷۷ جدول).

## اصلاح `deploy.mjs`

`.deps-hash` به `EXCLUDES` اضافه شد.

این فایل فقط روی سرور ساخته می‌شود و در ریپو نیست، پس `rsync --delete` هر بار
پاکش می‌کرد. نتیجه: هر استقرار بی‌دلیل کل `node_modules` را از لپ‌تاپ منتقل و
روی سایت **زنده** جایگزین می‌کرد. با این اصلاح، چون hash لاک‌فایل روی هر سه
سرور با لوکال یکی بود، انتقال کاملاً رد شد.

این ضمناً یک ریسک پنهان را هم خنثی کرد: سرور مای‌مونتا Node 20 دارد و لپ‌تاپ
Node 22 — انتقال `node_modules` می‌توانست باینری‌های native را خراب کند.

## گواهی HTTPS — مشکل اصلی

هر سه دامنه پشت CDN (WCDN) بودند و CDN با گواهی خودش TLS را terminate می‌کرد.
گواهی روی خود سرور وضعیت خوبی نداشت:

| دامنه | گواهی origin قبل از اصلاح |
|---|---|
| bartar-janebi.com | **۳۷ روز بود منقضی شده** (۵ جولای) |
| mymonta.ir | ۵ روز تا انقضا |
| mahamprint.com | معتبر، ولی تمدید **دستی** (DNS-01) |

یعنی اگر ابر CDN خاموش می‌شد، برتر جانبی بلافاصله برای همه‌ی بازدیدکننده‌ها
با خطای گواهی منقضی می‌شکست.

> نکته: عددهایی که از بیرون دیده می‌شد (۵ و ۹ و ۴۴ روز) مال گواهی **CDN** بود
> نه سرور. این دو را نباید با هم اشتباه گرفت.

### چرا ACME کار نمی‌کرد

هر سه کانفیگ nginx این الگو را داشتند:

```nginx
server {
    listen 80;
    location /.well-known/acme-challenge/ { root /var/www/letsencrypt; }  # مرده
    return 301 https://...;                                              # این اول اجرا می‌شود
}
```

`return` در سطح server کل location‌ها را دور می‌زند، پس مسیر ACME هیچ‌وقت سرو
نمی‌شد. همین باعث شده بود مهام‌پرینت مجبور شود سراغ DNS-01 دستی برود.

**اصلاح:** ریدایرکت به داخل `location /` منتقل شد تا `location ^~
/.well-known/acme-challenge/` بتواند match کند:

```nginx
location ^~ /.well-known/acme-challenge/ {
    root /var/www/letsencrypt;
    default_type "text/plain";
}
location / { return 301 https://...; }
```

### وضعیت نهایی

هر سه گواهی Let's Encrypt با تمدید **خودکار** (`webroot` + `certbot.timer`)،
پوشش apex و www:

```
mahamprint.com     تا ۱۰ نوامبر ۲۰۲۶   authenticator = webroot
bartar-janebi.com  تا  ۹ نوامبر ۲۰۲۶   authenticator = webroot
mymonta.ir         تا  ۹ نوامبر ۲۰۲۶   authenticator = webroot
```

نکته‌ی ترتیبی: CDN دامنه‌های برتر جانبی و مای‌مونتا درخواست HTTP را به origin
عبور می‌داد، پس گواهی آن دو **قبل** از خاموش کردن ابر صادر شد. ولی CDN
مهام‌پرینت درخواست HTTP را در لبه ریدایرکت می‌کرد، پس گواهی آن فقط **بعد** از
خاموش شدن ابر قابل صدور بود.

## سایر تنظیمات nginx

- `http2` روی برتر جانبی و مای‌مونتا فعال شد (قبلاً فقط `listen 443 ssl`).
- سرو مستقیم `/_next/static/` و `/assets/` توسط nginx برای برتر جانبی اضافه شد
  (مثل مهام‌پرینت). مای‌مونتا از قبل داشت.
- ⚠️ `http2 on;` در nginx 1.24 وجود ندارد (از 1.25 اضافه شده). سینتکس درست روی
  این سرورها `listen 443 ssl http2;` است.
- از هر کانفیگ قبل از تغییر بکاپ گرفته شد: `/root/*.nginx.bak-*`

## HSTS — بالا آمدن پلکانی

اول `max-age=300` منتشر شد چون گواهی‌ها وضعیت نامشخصی داشتند. اگر گواهی زیر
HSTS منقضی شود مرورگر **هارد-فیل** می‌کند و کاربر حتی نمی‌تواند رد شود.

بعد از اینکه هر سه گواهی خودتمدیدشونده شدند، مقدار به `31536000` (یک سال)
رسید و هر ۴ سایت دوباره دیپلوی شدند.

## کلیدهای IndexNow

هر سایت کلید جداگانه در `.env` خودش (از `.env` بکاپ گرفته شد: `.env.bak-indexnow-*`):

```
9dm.ir             384b45c63ea20788ea2a5776437978e3
bartar-janebi.com  20c6396f48013fd739f8c260da9875a5
mahamprint.com     d5f010ff6cbd153db2025949166c6155
mymonta.ir         26a024269f17b8fb9988dada21b04436
```

## تأیید نهایی (بعد از خاموش شدن ابرها)

هر سه دامنه مستقیم از سرور سرو می‌شوند (`server: nginx/1.24.0`، بدون WCDN):

```
HTTP/2 200 · HSTS max-age=31536000 · بدون X-Powered-By
/ · /products · /robots.txt · /sitemap.xml · /indexnow-key.txt  → همه 200
http → https  301  ·  www  200
```

## دو تله‌ای که خوردیم

**۱. دیپلوی مای‌مونتا وسط build قطع شد** (محدودیت زمانی ابزار). سایت سرپا ماند
چون پروسه‌ی قدیمی در حافظه بود، ولی `.next` نصفه بود و `BUILD_ID` نداشت —
یعنی اگر pm2 ری‌استارت می‌شد بالا نمی‌آمد. با اجرای دوباره‌ی کامل حل شد.
**درس:** بعد از هر استقرار، وجود `.next/BUILD_ID` را چک کنید.

**۲. پسورد دیتابیس `@` خام دارد.** `psql` روی **اولین** `@` می‌شکند ولی Node و
Prisma روی **آخرین** پارس می‌کنند — برای همین بکاپ و migration درست کار کرد
ولی هر اسکریپت `psql` مستقیم می‌شکند. برای کار با psql باید اجزای اتصال جدا
پاس داده شوند (`PGHOST`/`PGUSER`/`PGPASSWORD`)، نه کل URL.

---

## باقی‌مانده — وابسته به داده، نه کد

این موارد در آدیت پیدا شدند ولی عمداً انجام **نشدند**، چون داده‌ای هستند و
هر یک از ۵ سایت مقصد داده‌ی خودش را دارد:

- [ ] **۱۰۰٪ تصاویر محصول از `mahamprint.com` هات‌لینک می‌شوند.** هر ۲۴۶۵ محصول
      دارای تصویر، `mainImage` روی دامنه‌ی شخص ثالث دارد — صفر تصویر روی دامنه‌ی
      خودمان. اگر آن سایت hotlink را ببندد، rich result و Google Images با هم می‌روند.
- [ ] **ابعاد تصویر ۶۰۰×۶۰۰ است**، زیر ۸۰۰×۸۰۰ توصیه‌شده‌ی Merchant Center.
- [ ] **۲۷۷ محصول فعال هیچ تصویری ندارند** → `image` فیلد الزامی Product schema
      است، این صفحات واجد شرایط rich result نیستند.
- [ ] **جدول `ProductImage` عملاً خالی است (۳ رکورد)** → گالری هیچ‌وقت پر نمی‌شود
      و هر محصول فقط ۱ تصویر دارد (توصیه: حداقل ۳).
- [ ] **۹۷.۷٪ اسلاگ‌ها خراب‌اند** — ۲۶۷۸ از ۲۷۴۲ با خط تیره شروع می‌شوند
      (`----hp-pro-mfp-m28a`)؛ عنوان فارسی موقع اسلاگ‌سازی حذف شده. اصلاحش باید
      همراه ۳۰۱ redirect باشد چون sitemap همین آدرس‌ها را دارد.
- [ ] **جدول `Review` کاملاً خالی است** → `ratingCount` همه‌جا صفر →
      `aggregateRating` هیچ‌وقت تولید نمی‌شود و ستاره‌ی rich result نداریم.
- [ ] **۸۵۰ محصول بدون برند** → `brand` از schema حذف می‌شود.
- [ ] **`shortDescription` فقط روی ۱۹۹ از ۲۷۴۲ محصول** → توضیحات schema عمدتاً
      از `seoDescription` می‌آید که همان متن meta است، یعنی محتوای یکتای محصول
      تقریباً وجود ندارد.
- [ ] **`category` در دیتابیس گاهی غلط است** — محصول HP در دسته‌ی «شیرالات دوم»
      دیده شد. اسلاگ تک‌حرفی `ش` هم در جدول `Category` هست.
- [ ] **SKU/GTIN محصولات خالی است.** ستون‌ها ساخته شدند ولی پر کردنشان با دیتای
      هر سایت است.

---

## آنچه در بخش اول از قبل درست بود

`shippingDetails` و `hasMerchantReturnPolicy` کامل و درست‌اند (اکثر فروشگاه‌ها
ندارند). BreadcrumbList وجود دارد. canonical، OG و Twitter card تولید می‌شوند.
sitemap هر ۲۷۴۲ محصول را پوشش می‌دهد. `alt` روی همه‌ی تصاویر پر است.
SSR کامل است — گوگل‌بات برای دیدن محتوا به اجرای جاوااسکریپت نیاز ندارد.

> توجه: در بخش اول نوشته شده بود «`robots.txt` مسیرهای cart/checkout را درست
> می‌بندد». این در بخش دوم **عمداً تغییر کرد** — آن مسیرها حالا خزیدنی‌اند ولی
> `noindex` دارند. دلیلش در [بخش ۱۱](#۱۱-robotstxt-و-صفحات-خصوصی) توضیح داده شده.

---

## نکته‌ی جانبی

`CLAUDE.md` قدیمی است: مسیرهای فروشگاه الان `app/(shop)/` هستند، نه `app/store/`.
